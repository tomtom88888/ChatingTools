org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# compileSdk is 37 (see app/build.gradle.kts) which is one above the version
# Android Gradle plugin 9.1.0 was tested against. The ceiling is advisory;
# this acknowledges it instead of printing the warning on every build.
android.suppressUnsupportedCompileSdk=37
